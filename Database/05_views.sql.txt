CREATE OR REPLACE VIEW ROAD_REPORT
AS

SELECT

r.ROAD_NO,

r.ROAD_NAME,

r.ROAD_TYPE,

s.PCR,

s.IRI,

s.RUT,

GET_ROAD_STATUS
(
s.PCR,
s.IRI,
s.RUT,
s.CRACK
) STATUS


FROM ROADS r

JOIN SURVEY s

ON r.ROAD_ID=s.ROAD_ID;